workflows:
  ios-unsigned:
    name: iOS Unsigned Build
    environment:
      xcode: latest
      cocoapods: default
    scripts:
      - name: Install dependencies
        script: |
          cd collegeapp
          flutter pub get
      - name: Build iOS .app (unsigned)
        script: |
          cd collegeapp
          flutter build ios --no-codesign
    artifacts:
      - collegeapp/build/ios/iphoneos/*.app
